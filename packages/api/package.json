{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply browserplane-mcp-prod --remote",
    "db:migrate:local": "wrangler d1 migrations apply browserplane-mcp-prod --local",
    "db:studio": "drizzle-kit studio",
    "auth:generate": "pnpm dlx @better-auth/cli generate --config src/lib/auth.config.ts --output src/db/auth.schema.ts --yes"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "@modelcontextprotocol/sdk": "^1.20.2",
    "agents": "^0.2.16",
    "better-auth": "^1.3.28",
    "drizzle-orm": "^0.44.6",
    "hono": "^4.10.2",
    "nanoid": "^5.1.6",
    "zod": "3"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.3.28",
    "dotenv": "^17.2.3",
    "drizzle-kit": "^0.31.5",
    "wrangler": "^4.4.0"
  }
}